const initialState = {
  question: '',
  cards: [],
  answer: '',
  notes: '',
  readings: [],
};

const readingReducer = (state = initialState, action) => {
  switch (action) {
    default:
      return state;
  }
};

export default readingReducer;
