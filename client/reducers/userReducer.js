const initialState = {
  username: '',
  password: '',
  newUsername: '',
  newPassword: '',
  loggedinUser: {},
};

const userReducer = (state = initialState, action) => {
  switch (action) {
    default:
      return state;
  }
};

export default userReducer;
